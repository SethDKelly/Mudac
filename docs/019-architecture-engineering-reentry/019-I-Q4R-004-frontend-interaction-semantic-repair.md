---
type: Architecture Candidate Repair
title: 019-I Q4R-004 — Historical Front-End Interaction Semantic Repair
description: "Repairs the suspended front-end interaction candidate for current ADQ-008 comparison by translating pre-Phase-013 interaction assumptions to current Experience authority and separating reusable browser-state principles from historical framework/package selections."
status: stable
tags: [phase-019, architecture, q4, repair, browser, client, accessibility, degraded]
sources:
  - resource: ../canonical/architecture/frontend-interaction.md
  - resource: ../canonical/experience/mapping-authority-baseline.md
  - resource: ../canonical/experience/context-role-modes.md
  - resource: ../canonical/experience/judge-onboarding.md
  - resource: ../canonical/experience/judge-evaluation.md
  - resource: ../canonical/experience/live-operations.md
  - resource: ../canonical/experience/accessibility-resilience.md
  - resource: ../canonical/experience/status-feedback-recovery.md
  - resource: ../canonical/policies/anonymity-disclosure.md
  - resource: ../canonical/invariants/accessibility-semantic-parity.md
  - resource: ../canonical/invariants/truthful-authority-under-uncertainty.md
  - resource: ../canonical/architecture/identity-access-authority.md
  - resource: ../canonical/architecture/interface-command-concurrency.md
  - resource: ../canonical/architecture/offline-continuity-reconciliation.md
  - resource: ../canonical/architecture/artifact-export-publication-delivery.md
generated: { by: openai/gpt-5.6-sol, at: 2026-09-22T13:55:00-05:00 }
---

# Purpose

Complete Q4R-004 before the historical front-end interaction candidate participates in ADQ-008 comparison.

The historical source remains unchanged.

This repair translates reusable interaction hypotheses into current Phase-013 Experience language and removes package/framework choices from inherited authority.

# Candidate

> docs/canonical/architecture/frontend-interaction.md

Qualification before repair:

~~~text
Q2 / Q3 / Q4                 QUALIFIED_AFTER_REVISION
authority                    suspended candidate
comparison eligible          false
blocking repair              Q4R-004
stale binding                pre-Phase-013 interaction/mapping assumptions
technology revalidation      required
~~~

# Current semantic translation

Historical terms and assumptions translate as follows:

~~~text
historical Encounter context
  → Evaluation Occurrence

generic assigned judging task
  → Evaluation Obligation + logical Scorecard

role/mode as implicit authority
  → one current Competition-scoped Participation context
  + fresh contextual Access at protected boundaries

route hierarchy / wizard sequence
  → presentation/navigation only
  != dependence order
  != synchronization order
  != required explanation order

historical AUTH rules
  → accepted IAM authority

historical API rules
  → accepted CMD authority

historical SYNC rules
  → accepted RCV authority
~~~

Current Phase-013 mapping additionally requires:

- explanation order must not be hardened into a mandatory screen order;
- Judge-safe and Organizer-sensitive disclosure remain context-specific;
- phone-primary Judge work must preserve full legitimate semantics;
- accessibility, responsive and degraded paths preserve semantic parity;
- local working state, server-confirmed persistence and authoritative state remain distinguishable;
- result unknown is neither success nor failure;
- shared-device/context transitions cannot leak prior private state.

# Retained candidate hypotheses

The following hypotheses survive repair as current comparison inputs:

1. browser/navigation state is never domain authority;
2. client state should be partitioned by semantic responsibility rather than flattened into one undifferentiated store;
3. remote query/cache state is non-authoritative and freshness-capable;
4. eligible local Draft continuity is non-authoritative and context-bound;
5. high-consequence command outcomes are explicit and never optimistically final;
6. Participation/capacity context affects disclosure and navigation but does not create Access;
7. context switch/logout/shared-device handoff partitions or clears private client state;
8. Judge workflows require complete narrow/phone interaction;
9. Organizer workflows may use dense responsive views without semantic loss;
10. components should separate accessible primitives, semantic patterns, domain features and workspace composition;
11. multidimensional status must not collapse into a generic badge;
12. core interaction targets WCAG 2.2 AA-oriented semantic parity;
13. client validation assists while server validation remains authoritative;
14. conflict/recovery surfaces preserve evidence and uncertainty;
15. real-time push may accelerate refresh but is not a correctness dependency;
16. dense/tabular views may transform responsively without changing disclosure or authority;
17. rendering/route/feature failures are contained without inventing source-state loss;
18. client mechanisms remain replaceable behind accepted application contracts.

# Excluded obsolete or premature content

The repair does not carry forward as accepted authority:

- React as already selected framework;
- TypeScript as already selected implementation language;
- React Router or a specific routing mode;
- TanStack Query or any specific remote-cache package;
- IndexedDB or a specific browser persistence API/wrapper;
- a mandatory application shell or route hierarchy;
- Panel/Encounter terminology as current evaluation identity;
- role labels as permanent account roles or authority;
- route position, loader data, visible controls or cache possession as Access evidence;
- historical AUTH/API/SYNC identifiers as current authority;
- pre-Phase-013 explanation/dependence order as mandatory navigation flow;
- service workers, background sync, push transport, component library, state library, CSS system, build tool or telemetry SDK as already selected.

Existing React/TypeScript code is implementation/substrate evidence only. It may reduce later implementation cost, but does not itself settle ADQ-008.

# Repaired comparison statement

The historical candidate may now be compared only as this current hypothesis:

> **Use a client-rich browser architecture whose navigation and client state remain subordinate to current server authority; partition remote cache, command outcome, local Draft continuity, Participation/context hints and ephemeral view state; isolate disclosure/private state on context changes; preserve explicit pending/conflict/unknown/recovery states; support phone-primary Judge work, dense responsive Organizer work and WCAG 2.2 AA-oriented semantic parity; allow degraded capability reduction rather than weaker authority; and keep framework/router/query/storage/push/component mechanisms replaceable.**

# Repair decision

**Q4R-004 — COMPLETE.**

~~~text
historical source rewritten        NO
current semantic translation       YES
comparison eligible                YES
candidate adopted                  NO
technology selected                NO
~~~

Q4R-004 repairs evidence. It does not accept ADQ-008.
