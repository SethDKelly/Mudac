# Architecture

This directory is reserved for current accepted system/application architecture contracts and decisions.

It is intentionally sparse during Phase 004. System, application, data, synchronization, security, front-end, and AWS architecture are deferred to the later architecture phase.

Future architecture knowledge must reference and satisfy upstream canonical Concepts, mechanisms, policies, invariants, and experience contracts rather than redefining them for implementation convenience.